# jobact-backend

Placeholder Python backend for JobAct. Nothing is implemented yet — the
frontend in [`../frontend`](../frontend) currently simulates all
server-side behavior (persistence, auth, PDF generation, sync) with local
demo data.

## Setup

```bash
uv sync
```

## Run

```bash
uv run jobact-backend
```
