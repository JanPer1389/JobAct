"""Application-layer abstractions shared across the demo backend.

This package may depend on the standard library and other pure
application code, but must never import FastAPI or any other
framework/driver package.
"""
