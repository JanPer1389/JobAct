"""Identity bounded context: users, organizations, membership, sessions.

This is the tenancy foundation every later context (customers, visits,
reports, media) depends on for scoping data by organization.
"""
